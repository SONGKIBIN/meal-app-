# 변경 이력 (기능 요약)

이 문서는 백업 시점(2026-08-11) 기준으로 시스템에 포함된 전체 기능을 정리한 것입니다.
개발 진행 순서대로 정리되어 있으며, 복원이 필요할 때 어떤 기능이 들어있는 버전인지 확인하는 용도로 사용하세요.

## 2026-08-19 업데이트 (23차): 만족도 평가 추적 기능 + 식사 신청 "안 먹어요" 기능 추가

- **만족도 평가 추적 기능 추가**: 관리자 화면의 만족도 조사에서 평가 사유(내용)를 클릭하면
  정확히 언제 그 평가를 등록했는지(등록 일시) 확인할 수 있습니다. 지금까지 "월간" 집계 화면에는
  부서별 평균만 보였는데, "일별" 화면처럼 개별 직원의 별점·사유 상세 목록도 함께 볼 수 있도록
  추가했습니다.
- **식사 신청에 "안 먹어요" 기능 추가**: 중식/석식 각각에 신청 버튼과 별도로 "안 먹어요" 버튼을
  추가했습니다. 개인 직원은 버튼을 눌러 등록/취소할 수 있고, 도급(단체) 계정은 안 먹는 인원수를
  숫자로 직접 입력할 수 있습니다(신청 인원수와 별개로 동시에 입력 가능 — 예: 총원 40명 중 35명
  신청 + 5명 안 먹음 + 나머지 미정). 관리자·부서 운영자의 "일일 집계" 화면에 안 먹어요 인원
  카드와 명단이 추가되었고, 엑셀 다운로드에도 "안 먹어요" 시트가 함께 포함됩니다. 기존 "미신청"
  집계는 안 먹어요로 등록된 인원을 제외하고 순수하게 "아직 결정하지 않은" 인원만 보여주도록
  정리했습니다.

## 2026-08-16 업데이트 (22차): GitHub 저장소 코드 무결점 점검 반영

GitHub에 올려두신 코드를 직접 내려받아 처음부터 끝까지 다시 점검하고 발견한 문제들을 수정했습니다.

- **권한 회수 즉시 반영 범위 확대**: 21차에서 "관리자/통근버스 기사" 권한은 즉시 반영되도록
  고쳤는데, 일반 식사 신청·만족도 평가·통근버스 개인 신청 API는 빠져 있었습니다. 예를 들어
  퇴사 처리된 직원이 기존 로그인 토큰(최대 30일 유효)을 그대로 갖고 있으면, 퇴사 이후에도 식사
  신청/취소, 만족도 평가, 통근버스 승차 신청이 계속 가능했습니다. 이제 이 API들도 요청마다
  재직 여부를 다시 확인하도록 고쳐서, 재직 여부를 해제하면 재로그인 없이 즉시 차단됩니다.
  (`reservations.js`, `rating.js`, `bus.js`)
- **자정 직후 요일 계산 오류 수정**: 식사 신청 현황(주간) 조회 시 "오늘 날짜"를 서버 위치 기준
  (UTC)으로 계산하는 부분이 남아있어, 한국 시간 자정~오전 9시 사이에 요청하면 하루 전 날짜로
  잘못 계산될 수 있었습니다. 다른 곳과 동일하게 한국 시간 기준으로 계산하도록 통일했습니다.
  (`reservations.js`)
- **식단표 이미지 업로드 파일 형식 제한**: 식단표 이미지 등록 시 아무 파일이나 올릴 수 있었던
  부분을 사진 파일(PNG/JPEG/WEBP/GIF)만 허용하도록 제한하고, 허용되지 않는 파일을 올렸을 때
  화면에 정상적으로 오류 메시지가 뜨도록 고쳤습니다. (`menu.js`)
- **오프라인 캐시(PWA) 파일 목록 보완**: 인터넷 연결이 불안정할 때를 대비해 미리 저장해두는
  파일 목록에 일부 화면 스크립트(부서 운영자, 통근버스, 통근버스 관리자, 기사 화면)가 빠져있어
  해당 화면들이 오프라인에서 정상 동작하지 않을 수 있었습니다. 전체 화면 스크립트를 목록에
  추가했습니다. (`service-worker.js`)
- **도급(단체) 계정 인원수 입력 오류 시 안내 문구 추가**: 부서 운영자/관리자가 도급 계정의 신청
  인원수를 숫자로 직접 입력할 때, 잘못된 값(음수 등)을 입력하면 아무 반응 없이 조용히 무시되던
  것을 고쳐서 안내 문구가 뜨도록 했습니다. (`admin.js`, `manager.js`)

## 2026-08-16 업데이트 (21차): 보안 점검 결과 반영 (외부 코드 진단 후속 조치)

외부에서 받은 코드 진단 결과를 바탕으로 3가지를 수정했습니다.

- **입력값 검증 부족으로 인한 데이터 조작 가능성 수정**: 일부 API가 요청 값(직원 사번, 알림 구독
  주소)이 문자열인지 확인하지 않고 그대로 데이터베이스 조회 조건에 사용하고 있었습니다. 악의적인
  요청으로 의도하지 않은 다른 직원의 신청 내역이 조작될 가능성이 있어, 값이 문자열인지 먼저
  확인하도록 고쳤습니다. 해당하는 곳: 부서 운영자/관리자의 "신청 강제 처리" API
  (`manager.js`, `admin.js`), 알림 구독/해지 API(`push.js`), 통근버스 관리자의 승차 취소 API
  (`busAdmin.js`).
- **권한 회수가 즉시 반영되지 않던 문제 수정**: 관리자가 어떤 직원의 "관리자" 또는 "통근버스 기사"
  권한을 회수해도, 그 직원이 이미 로그인해서 가지고 있는 토큰(최대 30일 유효)으로는 회수 후에도
  계속 접근이 가능했습니다. 이제 다른 권한(부서 운영자, 통근차량 관리 관리자)처럼 매 요청마다
  데이터베이스에서 권한을 다시 확인하도록 고쳐서, 권한을 회수하거나 퇴사 처리(재직 여부 해제)하면
  재로그인 없이도 즉시 접근이 차단됩니다.
- **통근버스 차량 운행 기본값을 README 안내와 일치하도록 되돌림**: 17차에서 "모든 날짜 기본 운행,
  관리자는 취소만" 방식으로 바꿨었는데, README에는 계속 "평일 기본 운행, 특근일·대체휴무일·
  토요일·일요일은 관리자가 직접 켜줘야 운행" 이라고 안내되어 있어 실제 동작과 문서가 어긋나 있었습니다.
  사용자 확인 결과 평일은 상시 운행이 맞고, 주말·공휴일은 README 방식(관리자가 직접 지정)이 맞다고
  하여, 코드를 README 기준으로 되돌렸습니다. [통근차량 관리 → 운행일 지정] 화면에 선택한 날짜의
  기본값(운행함/운행 안 함)도 다시 표시됩니다.

## 2026-08-15 업데이트 (20차): "내가 타는 차" 등록을 다시 트립타입당 1개 고정 방식으로 변경 + 정시퇴근/연장퇴근 중 하루 하나만 선택

- **요일별 등록 폐지, 트립타입당 1개만 고정 등록**: 18차에서 도입했던 요일별(월~금) 등록 방식을
  없애고, 출근/정시퇴근/연장퇴근 각각 차량을 한 번만 등록하면 계속 고정되도록 되돌렸습니다. "내가
  타는 차" 영역에 "수정" 버튼이 생겨 언제든 등록 내용을 바꿀 수 있습니다.
- **출근은 그대로 자동 탑승**: 출근은 기존과 동일하게 평일(공휴일 제외)마다 등록된 차량으로 자동
  탑승 처리되며, 못 타는 날만 취소하면 됩니다.
- **정시퇴근/연장퇴근은 하루에 하나만 선택**: 퇴근은 하루에 한 번뿐이므로, 정시퇴근/연장퇴근은 더
  이상 자동으로 탑승 처리되지 않습니다. 대신 그날 아래 주간 신청에서 정시퇴근 또는 연장퇴근 중 하나를
  선택(신청)하면 되고, 이미 차량이 등록되어 있으므로 차량을 다시 고를 필요 없이 버튼 한 번으로
  신청됩니다. 정시퇴근을 신청한 상태에서 연장퇴근을 신청하면 정시퇴근 신청은 자동으로 취소되며(그
  반대도 마찬가지), 언제든 취소하거나 다른 쪽으로 바꿀 수 있습니다.
- **관리자/기사 화면도 동일하게 반영**: 관리자용 신청 현황/집계, 기사 모드의 당일운행·주간
  운행명령·월별 집계 화면 모두 새 규칙(정시퇴근/연장퇴근은 명시적으로 신청한 사람만 집계)에 맞게
  계산됩니다.
- (배포 참고) 18차에서 요일별로 여러 개 등록해두었던 데이터가 있다면, 배포 서버에서
  `MONGODB_URI="..." node server/scripts/migrate-busDefaultRide-single.js` 를 한 번 실행해
  직원당 트립타입별로 1개만 남기고 정리해주세요(이미 트립타입당 1개씩만 등록했던 경우에는 실행하지
  않아도 문제 없습니다).

## 2026-08-15 업데이트 (19차): 기사모드 [주간 운행명령] 화면에 탑승자 명단 추가

- **주간 화면에서도 탑승 인원·명단 확인 가능**: 기존에는 [주간 운행명령] 화면에서 차량별/요일별
  운행여부(운행함/운행안함)만 볼 수 있었는데, 이제는 운행하는 칸마다 탑승 인원수와 함께 탑승자의
  사번·부서·이름이 함께 표시됩니다. [당일운행] 화면을 매번 날짜를 바꿔가며 확인하지 않아도, 한 주
  전체의 탑승 현황을 한 화면에서 바로 볼 수 있습니다.

## 2026-08-15 업데이트 (18차): "내가 타는 차" 기본 등록을 요일별 주간 그리드 방식으로 변경

- **요일별로 다른 차량 등록 가능**: 기존에는 출근/정시퇴근/연장퇴근 운행구분별로 기본 차량을 하나만
  등록할 수 있었는데, 이제는 월~금 요일별로 각각 다른 차량을 등록할 수 있습니다(예: 월·수·금은
  평택차량, 화·목은 공도차량). [통근버스] 화면의 "내가 타는 차" 등록 영역이 요일별 칸이 나열된 주간
  그리드 형태로 바뀌었고, 각 요일 칸에서 차량·정류장을 선택해 등록하거나 "해지" 버튼으로 그 요일의
  등록만 해지할 수 있습니다.
- **못 타는 날은 그대로 건별 취소**: 평소 등록해둔 요일에 특정 날짜만 못 타게 된 경우, 아래 주간
  신청 표에서 그 날짜만 개별적으로 "취소"하면 됩니다. 요일별 기본 등록 자체는 유지되어 다음 주부터
  다시 자동 적용됩니다.
- **관리자·기사 화면도 동일하게 반영**: 관리자용 신청 현황/집계, 기사 모드의 당일운행·주간
  운행명령·월별 집계 화면 모두 요일별 기본 등록을 정확히 반영해 탑승자 명단과 인원을 계산합니다.

## 2026-08-15 업데이트 (17차): 통근버스 출근/정시퇴근/연장퇴근 탭 분리 + 차량운행 기본값 변경

- **출근/정시퇴근/연장퇴근 탭 분리**: 세 운행구분이 한 화면에 섞여 있어 구분이 어렵다는 의견을 반영해,
  탭을 눌러 전환하는 방식으로 바꿨습니다. 적용된 화면: 직원용 [통근버스] 신청 화면(내가 타는 차 등록 +
  주간 신청/취소 모두 탭으로 전환), 관리자용 [통근차량 관리 → 신청 현황] 화면, 기사 모드의
  [당일운행]·[주간 운행명령] 화면. (관리자용 일일/월간 집계 화면은 이미 세 운행구분이 각각 별도 열로
  나뉘어 표시되고 있어 별도 탭 없이 그대로 두었습니다. 이 화면들도 탭으로 나누길 원하시면 말씀해주세요.)
- **차량운행 기본값을 "운행함"으로 변경**: 그동안은 평일만 기본적으로 운행하고 특근일·대체휴무일·
  토요일·일요일은 관리자가 [운행일 지정]에서 매번 켜줘야 했는데, 이제는 요일/공휴일 구분 없이 모든
  날짜가 기본적으로 "운행"으로 처리됩니다. 특정 날짜에 차량이 운행하지 않을 때만(회사 휴무일, 차량
  정비 등) [운행일 지정] 화면에서 체크를 해제해 취소하면 됩니다. 직원의 "내가 타는 차" 기본 등록이
  자동으로 탑승 처리되는 날은 이전과 동일하게 평일(공휴일 제외)로 유지되며, 주말·공휴일에는 차량이
  운행하더라도 직원이 그때그때 직접 신청해야 명단에 포함됩니다.

## 2026-08-15 업데이트 (16차): 통근버스 "기본 차량 등록" 방식으로 개편

15차에서 만든 통근버스 신청 방식(평일마다 매번 직접 신청)을 실제 사용 방식에 맞게 개편했습니다.

- **"내가 타는 차" 기본 등록**: [통근버스] 화면 상단에서 출근운행/정시퇴근운행/연장퇴근운행별로 평소
  타는 차량과 정류장을 한 번만 등록해두면, 그 뒤로는 평일(기본 운행일)마다 별도 신청 없이 자동으로
  탑승자 명단·인원 집계에 포함됩니다. 도급(단체) 계정은 인원수도 함께 등록합니다.
- **못 타는 날만 취소**: 평소엔 자동으로 타는 것으로 처리되므로, 특정 평일에 못 타게 된 경우에만 그
  날짜의 "취소" 버튼을 누르면 됩니다. 기본 등록 자체는 그대로 유지되어 다음 날부터 다시 자동 적용됩니다.
- **공휴일/토요일/일요일은 그대로 개별 신청**: 관리자가 그 차량의 운행을 켜준 특근일 등에는 기본 등록이
  자동 적용되지 않으며, 그날 타고 싶은 직원은 이전처럼 직접 신청해야 합니다.
- **관리자 화면**: "신청 현황"에 어떤 탑승자가 기본 등록에 의한 자동 탑승인지, 그날 직접 신청한 것인지
  "구분" 열로 표시되며, 두 경우 모두 관리자가 대신 취소할 수 있습니다. 일일/월간 집계·엑셀 다운로드에도
  기본 등록 탑승자가 정확히 합산됩니다.
- **기사 모드 확대**: 기사 모드에 "당일운행"(본인 차량 운행일지 기록 + 3대 전체 탑승자 명단·인원 확인),
  "주간 운행명령"(이번 주 각 차량의 요일별 운행 여부를 미리 확인), "월별 집계"(차량별 날짜별 탑승 인원
  집계) 3개 탭이 추가되었습니다. 다른 기사님이 운행하는 차량을 포함해 3대 전체의 탑승자 이름까지 모든
  기사가 함께 확인할 수 있습니다.
- 전일 운행일지 이메일(매일 08:00 발송)에도 기본 등록 탑승자가 정확히 반영되도록 수정했습니다.
- (내부 수정) 담당 차량이 지정된 통근 차량 관리 관리자 화면에서, 직원이 담당 범위 밖 다른 차량으로 그날만
  바꿔 탄 경우에도 인원 집계가 정확히 계산되도록 하고, 관리자가 실수로라도 담당 범위 밖 차량의 신청을
  취소할 수 없도록 안전장치를 추가했습니다.

## 2026-08-15 업데이트 (15차): 통근버스 신청·관리 기능 추가 (비공개 개발자 모드)

- **통근버스 신청 기능 신규 추가**: 평택버스(평택·공도·내리), 공도버스(공도·내리), 안성버스(안성시내)
  3개 코스(코스는 관리자가 추가/수정 가능)에 대해 출근운행·정시퇴근운행·연장퇴근운행별로 정류장을 선택해
  승차 신청·취소를 할 수 있습니다. 도급(단체) 계정은 식수 신청과 동일하게 인원수를 직접 입력해 이용합니다.
- **평일 기본 운행, 특근일/휴일은 관리자가 지정**: 평일(월~금, 공휴일 제외)은 기본적으로 운행하는 것으로
  간주하고, 특근일·대체휴무일·토요일·일요일 등은 [통근차량 관리 → 운행일 지정]에서 관리자가 차량별로
  직접 켜줘야 운행됩니다.
- **비공개 개발자 모드**: 이 기능은 완성 전까지 마스터 관리자(사번 admin) 계정과, 마스터가 권한을 부여한
  "통근 차량 관리 관리자"/"기사" 계정만 미리보기로 사용할 수 있습니다. 마스터 관리자가
  [관리자 → 개발자 모드]에서 "공개로 전환"을 눌러야 전 직원에게 "통근버스" 탭이 나타납니다.
  이 메뉴는 일반 관리자(role=admin) 화면에는 아예 나타나지 않습니다.
- **마스터 관리자 비밀번호 신규 도입**: 사번 admin 계정으로 로그인할 때부터 비밀번호를 추가로 입력해야
  합니다. 최초 비밀번호는 `MASTER_INITIAL_PASSWORD` 환경변수(기본값 `admin1234!`)이며,
  로그인 후 [관리자 → 개발자 모드]에서 반드시 원하는 비밀번호로 변경해주세요.
- **통근 차량 관리 관리자**: 마스터가 부여한 권한으로, [통근차량 관리] 화면에서 차량/코스 관리,
  날짜별 운행 여부 지정, 일일·월간 인원 집계 확인 및 엑셀 다운로드·인쇄, 개별 신청 취소(대신 처리)를
  할 수 있습니다. 담당 차량을 지정하면 그 차량만, 비워두면 전체 차량을 관리합니다.
- **기사 모드**: 마스터가 부여한 권한으로, [기사 모드] 화면에서 본인이 배정된 차량의 오늘(또는 지정한
  날짜) 운행 여부·신청 인원을 확인하고, 실제 운행 여부·탑승 인원·메모를 기록할 수 있습니다. 다른 차량의
  운행 현황도 참고용으로 함께 볼 수 있습니다(읽기 전용).
- **이메일 알림 신규 추가 (선택 기능)**: SMTP 정보(`SMTP_HOST` 등)를 등록하면 매일 08:00에 전날 통근버스
  운행일지(코스·차량·신청인원·실제 운행 여부·실제 인원)를 마스터 관리자와 통근 차량 관리 관리자 중
  이메일을 등록한 사람에게 자동으로 보내줍니다. 또한 식수신청 관리자도 본인 이메일을 등록하면 기존 웹
  푸시 알림과 별개로 매일 중식/석식 신청 집계를 이메일로도 받아볼 수 있습니다. SMTP 정보를 등록하지
  않으면 이메일 기능만 자동으로 비활성화되고 나머지 기능은 동일하게 동작합니다.

## 2026-08-15 수정 (15차 패치 3): "공개로 전환" 시 강제 로그아웃되던 문제 수정

- 15차 패치 2에서 비밀번호 오류 메시지를 화면에 표시하도록 고쳤지만, 실제로는 비밀번호가 틀렸을 때
  서버가 401 응답을 내려주면 화면에 안내 문구가 뜨기도 전에 "로그인 세션 만료"로 잘못 처리되어
  강제 로그아웃되고 로그인 화면으로 튕기는 문제가 남아있었습니다(사용자 실사용 확인으로 발견).
  "개발자 모드"의 공개/비공개 전환, 마스터 비밀번호 변경 요청은 비밀번호 값 자체가 틀린 경우에만
  401을 반환하도록 되어 있는데, 로그인 화면 이외의 모든 요청은 401을 무조건 "세션 만료"로 간주해
  강제 로그아웃시키는 공통 처리 로직 때문이었습니다. 이제 이 두 요청은 로그인 시 비밀번호 오류를
  처리하는 방식과 동일하게, 강제 로그아웃 없이 오류 메시지만 화면에 표시하도록 수정했습니다.
  (실제 로그인 세션이 만료된 경우는 여전히 정상적으로 로그아웃 후 로그인 화면으로 이동합니다.)
- 올바른 마스터 비밀번호를 입력했는데도 계속 이 증상이 반복된다면, 최초 비밀번호(`admin1234!`,
  또는 이미 변경하셨다면 그 비밀번호)가 맞는지, 그리고 서버가 이번 수정이 반영된 최신 zip으로
  재배포되었는지 확인해주세요.

## 2026-08-15 수정 (15차 패치 2): 개발자 모드 "공개/비공개 전환" 버튼 동작·표시 수정

- **"공개로 전환"이 안 되던 문제 수정**: 비밀번호 칸을 비운 채 버튼을 누르면 그동안은 화면 팝업(alert)으로만
  "비밀번호를 입력해주세요"를 띄우고 아무 반응이 없어 보였는데, 휴대폰 앱 화면에서는 이 팝업이 보이지
  않거나 놓치기 쉬웠습니다. 이제는 팝업 대신 비밀번호 입력칸 바로 아래에 빨간 안내 문구로 표시되고,
  입력칸에 자동으로 포커스가 이동합니다. 비밀번호를 올바르게 입력하면 정상적으로 공개/비공개 전환됩니다.
- **"비공개로 전환" 버튼이 파란색으로 표시되지 않던 문제 수정**: 그동안은 현재 상태와 상관없이 "공개로
  전환" 버튼이 항상 파란색, "비공개로 전환" 버튼이 항상 회색으로 고정되어 있었습니다. 이제 현재 상태가
  "공개"이면 "비공개로 전환" 버튼이 파란색(활성)으로, 반대로 "비공개" 상태이면 "공개로 전환" 버튼이
  파란색으로 표시되어 지금 상태와 다음에 누를 버튼을 한눈에 알 수 있습니다.

## 2026-08-15 수정 (15차 패치): "개발자 모드" 탭 표시 문제 + 직원 관리 표 레이아웃 수정

- 마스터 관리자로 로그인한 직후(새로고침 없이)에는 관리자 화면에 "개발자 모드" 탭이 나타나지 않던
  문제를 수정했습니다. 이제 로그인할 때마다 마스터 관리자 여부를 다시 확인해 탭을 정확히 표시합니다.
  (기존에 접속해 있던 세션은 로그아웃 후 다시 로그인하면 반영됩니다.)
- 직원 관리 표의 열 너비를 정리했습니다: '권한' 열이 지나치게 넓고 '재직' 열이 좁던 것을 균형있게
  재배분했고, '수정'/'삭제' 버튼이 두 줄로 줄바꿈되던 것을 한 줄에 나란히 표시되도록 고쳤습니다.
- [개발자 모드 → 통근버스 권한 부여]에서 '배정 차량'을 선택해도 저장이 되지 않던 문제를 수정했습니다.
  '기사' 체크박스를 함께 켜지 않은 채 차량만 고르면 저장 시 배정이 자동으로 취소되던 것이 원인이었고,
  이제 차량을 선택하면 '기사' 체크박스가 자동으로 함께 켜지도록 고쳤습니다. 또한 등록된 차량이 아직
  없을 때는 안내 문구가 표시되고, 저장 후에는 목록이 새로고침되어 저장된 값을 바로 확인할 수 있습니다.

## 2026-08-15 업데이트 (14차): 만족도 평가 가능 시간을 익일 새벽까지로 확대

- **평가 참여율을 높이기 위해 평가 가능 시간을 대폭 확대**: 중식은 당일 12:00 ~ 익일 08:00, 석식은
  당일 17:00 ~ 익일 08:00(한국 시간) 사이 언제든 평가할 수 있습니다 (기존에는 중식 12:00~15:00,
  석식 17:00~20:00으로 당일 안에서만 가능했음).
- **자정을 넘긴 새벽 평가는 자동으로 "전날" 끼니로 기록**: 예를 들어 다음날 새벽 3시에 평가해도,
  그 평가는 어제 먹은 중식/석식에 대한 평가로 정확히 기록되어 관리자 화면의 부서별 집계·일별 목록에
  올바른 날짜로 반영됩니다. 화면에는 "당일 12:00 ~ 익일 08:00"처럼 안내 문구가 표시됩니다.
- 이 외 동작(신청한 사람만 평가 가능, 이유 필수 입력, 관리자 전용 집계 화면 등)은 이전과 동일합니다.

## 2026-08-14 업데이트 (13차): 만족도 평가 카드 항상 표시 + 평가 가능 시간 확대

- **평가 카드는 항상 표시**: "내 식사 신청" 화면의 만족도 평가 카드가 더 이상 시간대 밖에서 숨겨지지
  않고 항상 보입니다. 다만 식사를 신청하지 않았거나 평가 가능 시간이 아니면 별점·이유 입력칸과
  "평가 등록" 버튼이 비활성화되고, "OO을 신청하면 평가할 수 있습니다" 또는 "평가 가능 시간: OO:OO ~
  OO:OO" 같은 안내 문구가 표시됩니다.
- **평가 가능 시간 확대**: 중식은 12:00~15:00, 석식은 17:00~20:00(한국 시간)으로 평가 가능 시간대가
  넓어졌습니다 (기존 중식 12:00~13:00, 석식 17:00~18:00에서 변경).

## 2026-08-14 업데이트 (12차): 식사 만족도 별점 평가 기능 추가

- **직원용 별점 평가**: 식사를 신청한 직원은 중식은 12:00~13:00, 석식은 17:00~18:00(한국 시간) 사이에만
  "내 식사 신청" 화면에 나타나는 평가 카드에서 별 1~5개로 만족도를 남길 수 있습니다. 신청하지 않았거나
  평가 시간대가 아니면 카드 자체가 보이지 않습니다. 별 1개=20점, 2개=40점, 3개=60점, 4개=80점,
  5개=100점으로 자동 환산됩니다.
- **이유(사유) 필수 입력**: 별점을 누른 이유를 반드시 입력해야 "평가 등록" 버튼이 정상 처리됩니다
  (이유 없이는 서버에서도 등록을 거부합니다). 평가 시간대 안에서는 다시 눌러 별점/이유를 수정할 수
  있습니다.
- **관리자 전용 집계 화면**: [관리자] → [만족도 조사] 탭(신규, 관리자 화면에서만 보이고 부서 운영자
  화면에는 없음)에서 날짜별로 부서별 응답인원·평균점수·별점 분포(1~5개씩 몇 명)를 확인하고, 개별 응답과
  이유(사유)까지 목록으로 볼 수 있습니다. '월간' 보기에서는 한 달 치를 부서별로 집계해서 볼 수 있습니다.
- **엑셀 다운로드/인쇄**: 일별·월간 화면 모두 '엑셀 다운로드'로 부서별 집계 시트 + 개별 평가 상세(이유
  포함) 시트가 담긴 파일을 받거나, '인쇄' 버튼으로 화면을 바로 인쇄할 수 있습니다.

## 2026-08-13 업데이트 (11차): 관리자 직원 관리 화면 엑셀 다운로드/인쇄 추가

- **직원 명단 엑셀 다운로드**: [관리자] → [직원 관리] 화면에 "엑셀 다운로드" 버튼이 추가되어, 사번/이름/부서/
  권한/구분(개인·도급)/총원(TO)/담당 부서(운영자)/재직여부가 담긴 엑셀 파일을 내려받을 수 있습니다.
  화면에서 선택한 "전체 / 재직자 / 퇴직자" 필터가 다운로드 파일에도 그대로 반영됩니다.
- **인쇄**: "인쇄" 버튼을 누르면 현재 화면에 보이는 직원 목록 표를 바로 인쇄할 수 있습니다.

## 2026-08-13 업데이트 (10차): 관리자·운영자 화면에서 미신청자 신청 처리 버튼 추가

- **미신청자 목록에 "신청" 버튼 추가**: 전체 관리자 화면과 부서 운영자 화면 모두 "신청 현황" 탭에서
  중식/석식 미신청자 목록의 각 행(인원수 칸 오른쪽)에 "신청" 버튼이 새로 생겨, 사번을 직접 입력하지
  않고도 목록에서 바로 대신 신청 처리를 할 수 있습니다. 도급(TO 관리) 직원은 버튼을 누르면 신청할
  인원수를 입력하는 창이 뜨고(총원(TO)에 맞춰 기본값이 채워집니다), 개인 직원은 바로 1명으로 신청
  처리됩니다.
- 신청 완료된 건을 취소하는 기능은 기존과 동일하게 "신청 현황" 표의 각 행에 있는 "취소" 버튼으로
  처리하며, 이번 업데이트로 미신청자 신청(추가) + 신청자 취소를 모두 목록에서 클릭 한 번으로 처리할
  수 있게 되었습니다. (화면 위쪽의 사번 직접 입력 신청 칸은 그대로 남아있어 필요할 때 계속 쓸 수
  있습니다.)

## 2026-08-13 업데이트 (9차): 부서별 운영자(제한된 관리자) 권한 추가

- **부서 운영자 역할 신설**: 직원 관리 화면에서 특정 직원에게 "운영자" 권한을 부여하고, 담당 부서를
  추가로 지정할 수 있습니다. 운영자는 본인 소속 부서와 관리자가 지정해 준 부서에 한해서만 아래 기능을
  사용할 수 있고, 로그인하면 "내 식사 신청" 화면과 함께 "부서 운영자" 탭이 새로 나타납니다.
- **신청 현황 확인 및 대신 처리**: 담당 부서 소속 직원의 중식/석식 신청·미신청 현황을 확인하고,
  마감시간과 관계없이 신청/취소를 대신 처리할 수 있습니다 (기존 관리자의 "신청 현황" 화면과 동일하되
  담당 부서로 범위가 제한됩니다).
- **인원 관리 (수정만 가능)**: 담당 부서 소속 직원의 이름/구분(개인·도급)/총원(TO)/재직 여부를 수정할
  수 있지만, 새 직원을 등록하거나 삭제(퇴직 처리)할 수는 없습니다. 부서 이동이나 관리자·운영자 권한
  부여도 할 수 없습니다 (전체 관리자만 가능).
- **일일/월간 집계 + 엑셀 다운로드/인쇄**: 담당 부서 범위로 필터링된 일일 집계, 월간 집계를 확인하고
  기존 관리자 화면과 동일하게 엑셀로 다운로드하거나 인쇄할 수 있습니다.
- 담당 부서는 관리자가 직원 관리 화면에서 언제든 추가/변경할 수 있으며, 변경 사항은 운영자가 다시
  로그인하지 않아도 즉시 반영됩니다. 담당 부서는 등록된 부서명 목록에서 원하는 만큼 체크박스로
  선택할 수 있고(2개, 3개 이상도 자유롭게 선택 가능), 목록에 없는 새 부서명은 입력 후 "추가" 버튼으로
  체크박스를 새로 만들어 선택할 수 있습니다.

## 2026-08-11 업데이트 (8차): 아이폰/아이패드 홈 화면 설치 지원

- **아이폰·아이패드 "앱 설치" 버튼 동작 개선**: 안드로이드/PC 크롬에서는 기존처럼 버튼을 누르면 브라우저가
  바로 설치를 진행하지만, 아이폰·아이패드의 Safari는 이 자동 설치 기능을 지원하지 않아 지금까지는 버튼이
  아예 나타나지 않았습니다. 이번 업데이트로 아이폰·아이패드에서 접속하면 "앱 설치" 버튼이 나타나고,
  누르면 Safari 공유 버튼 → "홈 화면에 추가"로 설치하는 방법을 3단계로 안내하는 팝업이 뜹니다.
- **설치 후 전체화면으로 실행**: 홈 화면에 추가한 뒤 아이콘으로 실행하면 Safari 주소창 없이 다른 앱처럼
  전체화면으로 열리고, 홈 화면에는 "신흥정밀 식사신청"이라는 이름으로 표시됩니다.
- 이미 홈 화면에 설치되어 실행 중인 경우(아이폰/아이패드, 안드로이드, PC 공통)에는 "앱 설치" 버튼이
  자동으로 숨겨집니다.

## 2026-08-11 업데이트 (7차): 주말·공휴일 색상 구분 + 내방객(손님) 식수 신청

- **주말·공휴일 빨간색 표시**: 주간 캘린더에서 평일 신청 버튼은 기존처럼 파란색으로 보이고,
  토요일·일요일과 공휴일은 날짜·요일과 신청 버튼이 빨간색으로 구분되어 표시됩니다
  (신청완료 상태의 초록색 버튼과 마감으로 비활성화된 회색 버튼은 그대로 유지됩니다).
- **공휴일 자동 인식 + 관리자 등록**: 신정·삼일절·어린이날·현충일·광복절·개천절·한글날·크리스마스처럼
  매년 날짜가 고정된 양력 공휴일은 별도 설정 없이 자동으로 빨간색 표시됩니다. 설날·추석 연휴처럼 해마다
  날짜가 바뀌는 공휴일이나 대체공휴일·임시공휴일은 관리자 화면의 "설정" 탭 "공휴일 관리"에서 날짜와
  이름(선택)을 직접 등록/삭제할 수 있습니다. 등록된 공휴일은 일일·월간 집계 화면에도 함께 표시됩니다.
- **내방객(손님) 식수 신청**: 개인 직원이 중식/석식을 신청한 뒤, 신청 버튼 아래 "내방객" 입력칸에 함께
  식사할 손님·내방객 인원수(0~99명)를 추가로 등록할 수 있습니다. 신청·취소와 동일한 마감시간 규칙이
  적용되며, 신청을 취소하면 내방객 인원도 함께 초기화됩니다. 도급(단체) 계정은 총원(TO)으로 이미
  인원을 직접 입력하므로 이 기능을 사용하지 않습니다.
- **내방객 인원 집계 분리 표시**: 일일 집계·월간 집계·신청 현황 화면 모두 "일반직원 / 도급직원 / 내방객"
  인원을 각각의 카드로 구분해서 보여주고, 조리에 필요한 전체 식수(합계)에는 내방객 인원까지 포함해서
  계산합니다. 엑셀 다운로드(일일/월간)에도 내방객 인원 열과 합계가 함께 포함됩니다.

## 2026-08-11 업데이트 (6차): 로그인 자동 저장 + 접속 링크·QR 공유

- **로그인 시 사번/이름 자동 저장**: 로그인에 성공하면 사번과 이름을 이 기기에 저장해두었다가,
  다음에 접속했을 때 로그인 입력칸에 자동으로 채워줍니다.
- **접속 링크·QR코드 공유 기능 추가**: 로그인 후 화면 상단에 "접속 링크 공유" 버튼이 추가되어,
  누르면 사이트 주소를 복사하거나 QR코드를 스캔해서 다른 직원에게 바로 전달할 수 있습니다.

## 2026-08-11 업데이트 (5차): 관리자 전용 화면 범위 조정 (마스터 관리자만 제한)

- 3차 업데이트에서 "관리자는 식사 신청 메뉴 숨김 + 인원 제외" 처리를 모든 관리자 권한(role=admin) 계정에
  적용했는데, 이번에 **시스템 최초 설치 시 자동 생성되는 "마스터 관리자" 계정 1개에만** 적용되도록 범위를 좁혔습니다
  (기본 사번 `admin`, `.env`의 `ADMIN_EMPLOYEE_ID`로 변경했다면 그 사번).
- **마스터 관리자 계정**: 이전과 동일하게 "내 식사 신청" 메뉴 없이 관리자 화면만 보이고, 인원 집계에서도 제외됩니다.
- **직원 관리 화면에서 다른 직원에게 나중에 관리자 권한을 부여한 경우**: 처음 상태 그대로 관리자 화면과
  "내 식사 신청" 화면을 모두 사용할 수 있고, 등록 인원 요약과 미신청 인원 집계에도 정상적으로 포함되며 식사 신청도 가능합니다.
- 기존에 로그인해 있던 세션은 로그아웃 후 다시 로그인해야 이 구분이 정확히 반영됩니다.

## 2026-08-11 업데이트 (4차): 도급사 로그인 표시 방식 변경

- **도급사 로그인 시 "도급" 표시 제거, 부서명 + 이름으로 표시**: 화면 상단 사용자 표시와 "내 식사 신청" 화면의
  총원(TO) 카드에 더 이상 "도급" 배지나 "회사명:" 문구를 붙이지 않고, 예) "협력업체 OO건설"처럼
  부서명과 이름을 그대로 붙여서 보여줍니다.

## 2026-08-11 업데이트 (3차): 관리자 전용 화면 + 일일/월간 집계 개편

- **관리자 계정은 "내 식사 신청" 메뉴가 보이지 않고 관리자 화면만 표시**됩니다. 로그인하면 바로 관리자 화면으로 이동합니다.
  관리자 계정은 직원 관리 화면의 등록 인원 요약(개인 인원/도급사 총원/합계)과 "미신청 인원" 집계에서도 제외됩니다.
- **일일 집계에 신청자/미신청자 명단을 각각 표시**: 중식/석식별로 "신청자 명단"과 "미신청자 명단"을 구분된 표로 볼 수 있습니다.
- **일일 집계·월간 집계에 일반직원/도급직원 구분 인원 표시**: "일반직원 중식 00명, 도급직원 중식 00명, 중식 합계 000명" /
  "일반직원 석식 00명, 도급직원 석식 00명, 석식 합계 000명" 형태로 구분 인원과 통합 합계를 함께 보여줍니다.

## 2026-08-11 업데이트 (2차): 권한별 정렬 + 회사명 표기 + 업데이트 버튼

- **직원 목록 "권한별" 정렬 추가**: 정렬 드롭다운에 관리자 → 일반직원 순으로 묶어서 보는 옵션 추가
- **도급사 로그인 시 회사명 표기**: 화면 상단(사용자 배지)과 "내 식사 신청" 화면의 총원(TO) 카드에
  "회사명: OO건설" 형태로 회사명이 표시됨 (도급 배지도 함께 표시)
- **로그인 화면에 "업데이트 확인" 버튼 추가**: 화면이 예전 버전으로 보이거나 새 기능이 안 보일 때
  누르면 캐시와 서비스워커를 정리하고 최신 버전을 새로 받아옵니다 (특히 홈 화면에 앱으로 설치한 경우 유용)

## 2026-08-11 업데이트: 직원관리 고도화 + 도급팀 총원(TO) 자율 관리

- **등록 인원 요약**: 관리자 "직원 관리" 화면 상단에 개인 인원 / 도급사 총원(TO 합계) / 합계를 카드로 표시
- **직원 목록 정렬**: 부서별 / 가나다순(이름) / 사번별 / 재직자별 정렬을 드롭다운에서 즉시 전환 가능
- **재직자/퇴직자 구분 보기**: 전체·재직자·퇴직자 탭으로 필터링, 기본값은 재직자만 표시
- **완전 삭제(하드 삭제)**: 이미 퇴직 처리된(재직 여부 = 퇴사) 직원만 대상으로 DB 기록까지 영구 삭제 가능
  (재직 중인 직원은 완전 삭제 불가 — 먼저 퇴직 처리 필요). 퇴직자 목록에서 "복직" 버튼으로 재직 상태로 되돌리는 것도 가능
- **도급(단체) 계정 총원(TO) 셀프서비스**:
  - 도급팀으로 로그인하면 "내 식사 신청" 화면 상단에 현재 등록된 총원(TO)이 표시됨
  - 총원이 실제와 다르면 "총원 수정 요청" 버튼으로 희망 인원과 사유를 관리자에게 바로 전송 가능
  - 관리자 "직원 관리" 화면에 대기 중인 총원 수정 요청이 카드로 표시되며, 승인/거절 처리 가능
    (승인 시 즉시 TO에 반영, 거절 시 기존 TO 유지)
  - 각 날짜별 중식/석식 행에 "총원(TO) / 신청 인원 / 미신청 인원"을 함께 표시

## 기본 기능

- 사번 + 이름 로그인 (비밀번호 없음), 한국어 / English / 中文 다국어 지원
- 주 단위(월~일) 캘린더에서 중식/석식 개별 신청·취소
- 관리자: 직원 관리(엑셀 일괄 등록/템플릿 다운로드), 이름·사번·부서 검색 및 정렬
- 관리자: 신청 현황 강제 수정, 일일/월간 집계, 엑셀 다운로드, 인쇄
- 주간 식단표 게시(이미지 업로드 또는 텍스트)
- 관리자 데이터 백업(전체 직원/신청내역을 JSON 파일로 다운로드)
- PC/모바일 반응형 + PWA(홈 화면에 앱처럼 설치 가능)

## 알림 기능

- 휴대폰 웹 푸시 알림: 마감 임박 알림, 관리자 일일 집계 알림
- 외부 무료 크론 서비스(cron-job.org 등)가 `/api/cron/tick`을 주기적으로 호출해 알림을 발송
- VAPID 키가 설정되지 않아도 알림 기능만 비활성화될 뿐 나머지 기능은 정상 동작

## 마감시간 / 신청·취소 규칙

- 당일 신청·취소를 마감시간 전까지 자유롭게 허용 (기존에는 당일 취소가 불가능했으나 개선)
- **중식/석식 마감시간을 서로 다르게 설정 가능** (예: 중식 09:00, 석식 14:00),
  관리자 화면 "설정" 탭에서 재배포 없이 즉시 변경 가능
- 마감 임박 알림도 중식/석식 각각의 마감시간에 맞춰 별도로 발송

## 도급회사(단체) 계정

- 직원을 "도급" 구분으로 등록하면 개별 신청 대신 인원수를 숫자로 입력해
  일괄 신청·수정·취소(0 입력 시 취소) 가능
- 도급회사 계정에 **총 인원(TO)**을 설정해두면, (TO - 신청 인원)만큼을
  자동으로 "미신청 인원"으로 계산하여 신청현황/일일집계에 반영
- TO는 직원관리 화면에서 언제든 새로 입력하거나 수정 가능 (미설정 시 기존 방식으로 동작)
- 엑셀 일괄 등록 템플릿에도 "총원(TO, 도급만)" 열 포함

## 미신청 인원 확인

- "신청 현황" 탭: 중식/석식 미신청 직원 명단 및 인원수 확인
- "일일 집계" 탭에도 미신청 인원(중식/석식) 카드 추가로 함께 확인 가능

## 공지사항

- 공지 게시, 종료(직원 화면에서 숨김, 기록은 유지), **완전 삭제**(목록에서도 제거, 복구 불가) 가능

## 주간 식단표 자동 관리

- "이번 주/다음 주"는 저장된 날짜(월요일 기준) 그대로 조회되므로, 주가 바뀌면
  자동으로 다음 주 식단표가 이번 주로 표시되고 별도 작업 없이 그다음 주가 빈 화면으로 전환됨
- 8주(약 2개월)가 지난 지난 식단표는 서버 재시작 시 및 매일 1회 자동으로 완전 삭제
  (보관 기간은 `MENU_RETENTION_WEEKS` 환경변수로 조정 가능)

## QR / 접속 안내

- 접속 링크 QR코드 제공 (관리자가 직원들에게 배포용으로 활용 가능)

## 브랜딩 (신흥정밀)

- 신흥정밀(SHC) 로고를 헤더, 로그인 화면, 브라우저 파비콘, PWA 앱 설치 아이콘에 적용
- PWA 설치 시 표시되는 앱 이름을 "신흥정밀 식사 신청 시스템"으로 변경
- 브라우저 테마 색상(설치 시 상단바 등)을 로고의 레드 컬러(#C4162B)로 통일
